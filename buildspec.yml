version: 0.2
# try and load secrets as env vars 
env:
  secrets-manager:
    access_key: "arn:aws:secretsmanager:eu-central-1:654654627826:secret:access_key-GNbp0e"
    secret_key: "arn:aws:secretsmanager:eu-central-1:654654627826:secret:secrett_key-yU41MM"
phases:
  pre_build:
    commands:
      - docker build -t terraform-shell .
      - echo $access_key # is showing in cloudwatch as ***, ehich seems pretty okay  of cloudbuild logging 
  build:
    commands:
      - docker images
      - docker run terraform-shell /bin/pwd;/bin/ls
# trying to get it to do anything based on created docker image .